import React from 'react';
import AuthTemplate from "../AuthTemplate/AuthTemplate";

const Authentication = () => {
    return (
        <div>
            <AuthTemplate headerLabel = {"Войдите, чтобы продолжить"} buttonLabel = {"Войти"}/>
        </div>
    );
};

export default Authentication;
